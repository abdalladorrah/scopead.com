# خطة تحويل المشروع إلى ديناميكي

## 📋 نظرة عامة
هذه الخطة توضح كيفية تحويل جميع صفحات المشروع لتصبح ديناميكية وتقرأ المحتوى من ملف `project_data.js` الواحد.

---

## ✅ الحالة الحالية

### الصفحات التي تم تحويلها بالفعل:
1. ✅ **index.html** (الداشبورد) - **تم التحويل الآن**
2. ✅ **persona.html** - يستخدم `project_data.js`
3. ✅ **vision.html** - يستخدم `project_data.js`

### الصفحات التي تحتاج للتحويل:
1. ⏳ **budget.html** - يحتاج تحويل
2. ⏳ **strategy.html** - يحتاج تحويل
3. ⏳ **plan.html** - يحتاج تحويل
4. ⏳ **competitors.html** - يحتاج تحويل
5. ⏳ **creatives.html** - يحتاج تحويل
6. ⏳ **journey.html** - يحتاج تحويل
7. ⏳ **retention.html** - يحتاج تحويل
8. ⏳ **timeline.html** - يحتاج تحويل
9. ⏳ **future.html** - يحتاج تحويل
10. ⏳ **checklist.html** - يحتاج تحويل

---

## 🎯 ما تم إنجازه

### 1. إضافة قسم Dashboard إلى project_data.js
تم إضافة قسم كامل يحتوي على:
- معلومات Hero (العنوان والوصف)
- معلومات Countdown (العد التنازلي)
- المقاييس الرئيسية (Metrics)
- متوسط الفواتير (Tickets)
- روابط الوصول السريع (Quick Access)

### 2. تحويل index.html
- تم استبدال المحتوى الثابت بـ IDs ديناميكية
- إضافة JavaScript لقراءة البيانات من `project_data.js`
- دعم كامل للغة العربية والإنجليزية
- الحفاظ على وظيفة Countdown Timer

---

## 📝 خطوات التحويل للصفحات المتبقية

### النمط العام للتحويل:

#### الخطوة 1: إضافة البيانات إلى project_data.js
```javascript
// مثال: إضافة قسم جديد
sectionName: {
    // جميع البيانات بالعربية والإنجليزية
    title: { ar: "...", en: "..." },
    // ... إلخ
}
```

#### الخطوة 2: تعديل HTML
- استبدال النصوص الثابتة بـ IDs
- إضافة `<div id="..."></div>` للمحتوى الديناميكي
- إضافة `id="footer-text"` للفوتر

#### الخطوة 3: إضافة JavaScript
```javascript
<script src="project_data.js"></script>
<script>
    document.addEventListener('DOMContentLoaded', () => {
        const data = projectData.sectionName;
        // Render Arabic
        document.getElementById('id-ar').textContent = data.title.ar;
        // Render English
        document.getElementById('id-en').textContent = data.title.en;
        // Footer
        document.getElementById('footer-text').textContent = projectData.general.footerText;
    });
</script>
```

---

## 📊 تفاصيل التحويل لكل صفحة

### 1. budget.html
**البيانات المطلوبة:**
- ✅ موجودة في `projectData.budget`
- يحتاج فقط لربط HTML بـ JavaScript

**العناصر المطلوبة:**
- العنوان الرئيسي
- إجمالي الميزانية
- قائمة بنود الميزانية
- KPIs

---

### 2. strategy.html
**البيانات المطلوبة:**
- ✅ موجودة في `projectData.strategy`
- يحتاج فقط لربط HTML بـ JavaScript

**العناصر المطلوبة:**
- العنوان والوصف
- المقاييس (Metrics)
- الأعمدة (Pillars)
- North Star
- المعادلة

---

### 3. plan.html
**البيانات المطلوبة:**
- ✅ موجودة في `projectData.plan`
- يحتاج فقط لربط HTML بـ JavaScript

**العناصر المطلوبة:**
- عنوان Q1
- الأشهر والأسابيع
- استراتيجية المؤثرين

---

### 4. competitors.html
**البيانات المطلوبة:**
- ✅ موجودة في `projectData.competitors`
- يحتاج فقط لربط HTML بـ JavaScript

**العناصر المطلوبة:**
- العنوان والوصف
- قائمة المنافسين
- SWOT Analysis

---

### 5. creatives.html
**البيانات المطلوبة:**
- ✅ موجودة في `projectData.creatives`
- يحتاج فقط لربط HTML بـ JavaScript

**العناصر المطلوبة:**
- القنوات (Channels)
- الإعلانات (Ads)

---

### 6. journey.html
**البيانات المطلوبة:**
- ✅ موجودة في `projectData.journey`
- يحتاج فقط لربط HTML بـ JavaScript

**العناصر المطلوبة:**
- خطوات الرحلة (Steps)

---

### 7. retention.html
**البيانات المطلوبة:**
- ✅ موجودة في `projectData.retention`
- يحتاج فقط لربط HTML بـ JavaScript

**العناصر المطلوبة:**
- برامج الولاء
- العناصر (Items)

---

### 8. timeline.html
**البيانات المطلوبة:**
- ✅ موجودة في `projectData.timeline`
- يحتاج فقط لربط HTML بـ JavaScript

**العناصر المطلوبة:**
- الأسابيع والأنشطة

---

### 9. future.html
**البيانات المطلوبة:**
- ✅ موجودة في `projectData.future`
- يحتاج فقط لربط HTML بـ JavaScript

**العناصر المطلوبة:**
- العنوان والوصف
- Phase 1 و Phase 2

---

### 10. checklist.html
**البيانات المطلوبة:**
- ⚠️ **غير موجودة** - يحتاج إضافة إلى `project_data.js`

**العناصر المطلوبة:**
- قائمة المهام (Checklist Items)

---

## 🔧 الملفات المطلوب تعديلها

### ملفات JavaScript:
1. ✅ `project_data.js` - تم إضافة قسم Dashboard
2. ⏳ `project_data.js` - يحتاج إضافة قسم Checklist (إن لم يكن موجوداً)

### ملفات HTML:
1. ✅ `index.html` - تم التحويل
2. ⏳ `budget.html` - يحتاج تحويل
3. ⏳ `strategy.html` - يحتاج تحويل
4. ⏳ `plan.html` - يحتاج تحويل
5. ⏳ `competitors.html` - يحتاج تحويل
6. ⏳ `creatives.html` - يحتاج تحويل
7. ⏳ `journey.html` - يحتاج تحويل
8. ⏳ `retention.html` - يحتاج تحويل
9. ⏳ `timeline.html` - يحتاج تحويل
10. ⏳ `future.html` - يحتاج تحويل
11. ⏳ `checklist.html` - يحتاج تحويل + إضافة بيانات

---

## 📌 ملاحظات مهمة

### 1. البنية الموحدة
جميع الصفحات يجب أن تتبع نفس النمط:
- قسم `content-ar` للعربية
- قسم `content-en` للإنجليزية
- استخدام IDs ديناميكية
- ربط بـ `project_data.js`

### 2. Footer
جميع الصفحات تستخدم:
```html
<footer>
    <p id="footer-text"></p>
</footer>
```
ويتم ملؤه من `projectData.general.footerText`

### 3. Navigation
جميع الصفحات تستخدم `nav.js` الذي يعمل تلقائياً

### 4. اللغة
- استخدام `localStorage` لتخزين اللغة المفضلة
- التبديل بين العربية والإنجليزية

---

## 🎯 الأولويات

### المرحلة 1 (عاجل):
1. ✅ **index.html** - تم
2. ⏳ **budget.html** - مهم جداً
3. ⏳ **strategy.html** - مهم جداً

### المرحلة 2 (مهم):
4. ⏳ **plan.html**
5. ⏳ **competitors.html**
6. ⏳ **creatives.html**

### المرحلة 3 (مطلوب):
7. ⏳ **journey.html**
8. ⏳ **retention.html**
9. ⏳ **timeline.html**
10. ⏳ **future.html**
11. ⏳ **checklist.html**

---

## ✅ اختبار بعد التحويل

لكل صفحة، يجب التحقق من:
1. ✅ المحتوى يظهر بالعربية بشكل صحيح
2. ✅ المحتوى يظهر بالإنجليزية بشكل صحيح
3. ✅ التبديل بين اللغات يعمل
4. ✅ Footer يظهر بشكل صحيح
5. ✅ Navigation يعمل
6. ✅ التصميم لم يتأثر

---

## 📝 ملخص

**الحالة الحالية:**
- ✅ 3 صفحات محولة (index, persona, vision)
- ⏳ 10 صفحات تحتاج تحويل

**البيانات:**
- ✅ جميع البيانات موجودة في `project_data.js` (ما عدا checklist)
- ✅ البنية جاهزة للاستخدام

**الخطوات التالية:**
1. تحويل الصفحات المتبقية واحدة تلو الأخرى
2. إضافة بيانات checklist إذا لزم الأمر
3. اختبار شامل للمشروع

---

**تاريخ الإنشاء:** اليوم  
**آخر تحديث:** بعد تحويل index.html
